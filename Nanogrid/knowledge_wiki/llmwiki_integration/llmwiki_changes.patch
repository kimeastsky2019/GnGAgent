diff --git a/config.yaml b/config.yaml
index 1c4cd7d..5b5e0de 100644
--- a/config.yaml
+++ b/config.yaml
@@ -1,18 +1,17 @@
 # LLMWiki 설정
 project:
-  name: "샘플 금융시스템"
+  name: "NanoGrid AI System"
   # 분석 대상 소스 루트 (여러 개 지정 가능)
   source_roots:
     - "./sample"
   # 시스템 구분: 경로 패턴 → 계층명 (뷰어 좌측 트리 1단)
   layers:
     - { name: "기관계", match: "**/inst/**" }
-    - { name: "채널계", match: "**/channel/**" }
     - { name: "공통", match: "**/common/**" }
 
 parse:
   include: ["**/*.java", "**/*.xml"]
-  exclude: ["**/target/**", "**/build/**", "**/test/**", "**/generated/**"]
+  exclude: ["**/target/**", "**/build/**", "**/test/**", "**/generated/**", "**/channel/**"]
   # LLM 컨텍스트에 넣을 클래스당 최대 문자수 (초과 시 메서드 시그니처만)
   max_class_chars: 12000
 
diff --git a/llmwiki/server/app.py b/llmwiki/server/app.py
index d1deb86..1007184 100644
--- a/llmwiki/server/app.py
+++ b/llmwiki/server/app.py
@@ -26,6 +26,11 @@ store = DocStore(docs_dir=cfg.docs_dir)
 
 app = FastAPI(title=f"{cfg.project_name} — LLMWiki")
 
+# 나노그리드 Knowledge Wiki (/api/ng/*) — ngwiki wiki-server 프록시
+from .ng_proxy import router as ng_router  # noqa: E402
+
+app.include_router(ng_router, prefix="/api/ng", tags=["nanogrid"])
+
 _index_cache: dict[str, Any] = {"mtime": None, "index": None}
 
 # 소스 열람 한도 — 뷰어가 브라우저를 멈추게 하지 않도록 자른다
diff --git a/web/package.json b/web/package.json
index 59c4c89..a0e0e69 100644
--- a/web/package.json
+++ b/web/package.json
@@ -13,6 +13,7 @@
     "react": "^18.3.1",
     "react-dom": "^18.3.1",
     "react-markdown": "^9.0.1",
+    "recharts": "^3.10.1",
     "remark-gfm": "^4.0.0"
   },
   "devDependencies": {
diff --git a/web/src/App.tsx b/web/src/App.tsx
index 28d13a9..bbc1f7d 100644
--- a/web/src/App.tsx
+++ b/web/src/App.tsx
@@ -20,20 +20,53 @@ import {
 } from "./i18n";
 import Markdown from "./Markdown";
 import SourceBrowser, { type SourceTarget } from "./SourceBrowser";
+import {
+  NgAdmin, NgDocView, NgForecast, NgGov, NgInsights,
+  NgKnowledgeDb, NgModeTabs, NgMonitor, NgProjectSelect, NgSection,
+  ngModeForPath, type NgMode,
+} from "./NanoGrid";
 
 type Route =
   | { kind: "home" }
   | { kind: "program"; id: string }
   | { kind: "table"; name: string }
-  | { kind: "tables" };
+  | { kind: "tables" }
+  | { kind: "ng-monitor"; tab: "energy" | "ev" | "events" }
+  | { kind: "ng-forecast" }
+  | { kind: "ng-knowledge" }
+  | { kind: "ng-insights" }
+  | { kind: "ng-admin" }
+  | { kind: "ng-gov" }
+  | { kind: "ng-doc"; id: string };
 
 function parseRoute(path: string): Route {
   if (path.startsWith("/p/")) return { kind: "program", id: path.slice(3) };
   if (path.startsWith("/t/")) return { kind: "table", name: decodeURIComponent(path.slice(3)) };
   if (path === "/tables") return { kind: "tables" };
+  if (path === "/ng/monitor") return { kind: "ng-monitor", tab: "energy" };
+  if (path === "/ng/monitor/ev") return { kind: "ng-monitor", tab: "ev" };
+  if (path === "/ng/monitor/events") return { kind: "ng-monitor", tab: "events" };
+  if (path === "/ng/forecast") return { kind: "ng-forecast" };
+  if (path === "/ng/knowledge") return { kind: "ng-knowledge" };
+  if (path === "/ng/insights") return { kind: "ng-insights" };
+  if (path === "/ng/admin") return { kind: "ng-admin" };
+  if (path === "/ng/gov") return { kind: "ng-gov" };
+  if (path.startsWith("/ng/doc/")) return { kind: "ng-doc", id: path.slice(8) };
   return { kind: "home" };
 }
 
+function routePath(route: Route): string {
+  if (route.kind === "ng-monitor")
+    return route.tab === "energy" ? "/ng/monitor" : `/ng/monitor/${route.tab}`;
+  if (route.kind === "ng-forecast") return "/ng/forecast";
+  if (route.kind === "ng-knowledge") return "/ng/knowledge";
+  if (route.kind === "ng-insights") return "/ng/insights";
+  if (route.kind === "ng-admin") return "/ng/admin";
+  if (route.kind === "ng-gov") return "/ng/gov";
+  if (route.kind === "ng-doc") return `/ng/doc/${route.id}`;
+  return "";
+}
+
 export default function App() {
   const [route, setRoute] = useState<Route>(() => parseRoute(location.pathname));
   const [meta, setMeta] = useState<Meta | null>(null);
@@ -69,11 +102,24 @@ export default function App() {
   );
   const t = langValue.t;
 
+  // 사이드바 모드: 소스 분석(운영 소스 → 명세서·규제 준수) ↔ 데이터 지식화(데이터 → 지식베이스·위키)
+  const [mode, setMode] = useState<NgMode>(
+    () => ngModeForPath(location.pathname) ?? "source"
+  );
+
   const navigate = useCallback((path: string) => {
     history.pushState(null, "", path);
     setRoute(parseRoute(path));
+    const forced = ngModeForPath(path);
+    if (forced) setMode(forced);
   }, []);
 
+  const switchMode = useCallback((m: NgMode) => {
+    setMode(m);
+    // 탭 전환 시 해당 모드의 대표 화면으로 이동
+    navigate(m === "data" ? "/ng/monitor" : "/");
+  }, [navigate]);
+
   const openSource = useCallback((target: SourceTarget) => {
     setSource(target);
     setBrowserOpen(true);
@@ -120,7 +166,7 @@ export default function App() {
         <aside className="sidebar">
           <div className="brand-row">
             <div className="brand" onClick={() => navigate("/")}>
-              <span className="brand-mark">LW</span>
+              <img src="/gng-logo.png" alt="GnG" className="brand-logo" />
               <div>
                 <div className="brand-title">{meta?.project ?? "LLMWiki"}</div>
                 <div className="brand-sub">
@@ -135,33 +181,45 @@ export default function App() {
             <LangToggle lang={lang} onChange={setLang} />
           </div>
 
-          <input
-            className="search"
-            placeholder={t("searchPlaceholder")}
-            value={query}
-            onChange={(e) => setQuery(e.target.value)}
-          />
-
-          {hits ? (
-            <SearchResults hits={hits} onPick={(id) => navigate(`/p/${id}`)} />
+          <NgModeTabs mode={mode} onChange={switchMode} />
+
+          {mode === "source" ? (
+            <>
+              <NgProjectSelect />
+              <input
+                className="search"
+                placeholder={t("searchPlaceholder")}
+                value={query}
+                onChange={(e) => setQuery(e.target.value)}
+              />
+
+              {hits ? (
+                <SearchResults hits={hits} onPick={(id) => navigate(`/p/${id}`)} />
+              ) : (
+                <>
+                  <Tree tree={tree} route={route} onPick={navigate} />
+                  <NgSection activePath={routePath(route)} onPick={navigate} mode="source" />
+                </>
+              )}
+
+              <div className="side-actions">
+                <button className="tables-link" onClick={() => navigate("/tables")}>
+                  {t("tablesLink")}
+                </button>
+                <button
+                  className="tables-link"
+                  onClick={() => {
+                    setSource(null);
+                    setBrowserOpen(true);
+                  }}
+                >
+                  {t("sourceLink")}
+                </button>
+              </div>
+            </>
           ) : (
-            <Tree tree={tree} route={route} onPick={navigate} />
+            <NgSection activePath={routePath(route)} onPick={navigate} mode="data" />
           )}
-
-          <div className="side-actions">
-            <button className="tables-link" onClick={() => navigate("/tables")}>
-              {t("tablesLink")}
-            </button>
-            <button
-              className="tables-link"
-              onClick={() => {
-                setSource(null);
-                setBrowserOpen(true);
-              }}
-            >
-              {t("sourceLink")}
-            </button>
-          </div>
         </aside>
 
         <main className="content">
@@ -174,6 +232,13 @@ export default function App() {
           {route.kind === "table" && (
             <TableView name={route.name} onPick={navigate} onOpenSource={openSource} />
           )}
+          {route.kind === "ng-monitor" && <NgMonitor tab={route.tab} onNavigate={navigate} />}
+          {route.kind === "ng-forecast" && <NgForecast />}
+          {route.kind === "ng-knowledge" && <NgKnowledgeDb />}
+          {route.kind === "ng-insights" && <NgInsights onNavigate={navigate} />}
+          {route.kind === "ng-admin" && <NgAdmin onNavigate={navigate} />}
+          {route.kind === "ng-gov" && <NgGov />}
+          {route.kind === "ng-doc" && <NgDocView id={route.id} onNavigate={navigate} />}
         </main>
 
         {browserOpen && (
@@ -221,16 +286,18 @@ function Tree({
             <div key={tier.tier}>
               <div className="tree-tier">{tier.tier}</div>
               {tier.programs.map((p) => (
-                <button
-                  key={p.id}
-                  className={`tree-item ${activeId === p.id ? "active" : ""}`}
-                  onClick={() => onPick(`/p/${p.id}`)}
-                  title={p.urls.join(", ")}
-                >
-                  <span className={`dot ${p.has_doc ? "ok" : "todo"}`} />
-                  <span className="tree-item-name">{p.name}</span>
-                  <span className="tree-item-badge">{p.sql_count}</span>
-                </button>
+                <div key={p.id}>
+                  <button
+                    className={`tree-item ${activeId === p.id ? "active" : ""}`}
+                    onClick={() => onPick(`/p/${p.id}`)}
+                    title={p.urls.join(", ")}
+                  >
+                    <span className={`dot ${p.has_doc ? "ok" : "todo"}`} />
+                    <span className="tree-item-name">{p.name}</span>
+                    <span className="tree-item-badge">{p.sql_count}</span>
+                  </button>
+                  <ProgramSubmenu programId={p.id} onPick={onPick} />
+                </div>
               ))}
             </div>
           ))}
@@ -241,6 +308,37 @@ function Tree({
   );
 }
 
+/** 프로그램(예: 고객 정보 관리) 아래 세부 메뉴 — 명세서 · 테이블·쿼리 · AI-Gov 준수 체크 */
+function ProgramSubmenu({
+  programId,
+  onPick,
+}: {
+  programId: string;
+  onPick: (p: string) => void;
+}) {
+  const { lang } = useLang();
+  const tr = (ko: string, en: string) => (lang === "en" ? en : ko);
+  const items = [
+    { path: `/p/${programId}`, icon: "📄", label: tr("프로그램 명세서", "Program Spec") },
+    { path: "/tables", icon: "🗃", label: tr("테이블·쿼리", "Tables & Queries") },
+    { path: "/ng/gov", icon: "📜", label: tr("AI-Gov 준수 체크", "AI-Gov Compliance Check") },
+  ];
+  return (
+    <div className="ng-children">
+      {items.map((it) => (
+        <button
+          key={it.path}
+          className="tree-item ng-doc"
+          onClick={() => onPick(it.path)}
+        >
+          <span className="ng-icon">{it.icon}</span>
+          <span className="tree-item-name">{it.label}</span>
+        </button>
+      ))}
+    </div>
+  );
+}
+
 function SearchResults({
   hits,
   onPick,
diff --git a/web/src/styles.css b/web/src/styles.css
index 5ba5e0d..82a4871 100644
--- a/web/src/styles.css
+++ b/web/src/styles.css
@@ -568,3 +568,101 @@ th { background: #f1f3f7; font-weight: 650; white-space: nowrap; }
   .sb-body { flex-direction: column; }
   .sb-tree { width: 100%; flex: 0 0 40%; border-right: 0; border-bottom: 1px solid var(--line); }
 }
+
+/* ---------------------------------------------------------------- 나노그리드 */
+.ng-section { border-bottom: 1px solid rgba(255,255,255,0.08); padding-bottom: 8px; margin-bottom: 8px; }
+.ng-icon { width: 16px; display: inline-block; text-align: center; margin-right: 4px; font-size: 12px; }
+.ng-doc { padding-left: 26px; }
+.ng-page { max-width: 1100px; }
+.ng-title { margin: 0 0 4px; font-size: 22px; }
+.ng-kpis { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 12px; margin: 16px 0; }
+.ng-kpi { background: #fff; border: 1px solid #e2e8f0; border-radius: 10px; padding: 14px 16px; }
+.ng-kpi-label { font-size: 12px; color: #64748b; }
+.ng-kpi-value { font-size: 24px; font-weight: 700; color: #0f172a; }
+.ng-kpi-sub { font-size: 12px; color: #94a3b8; }
+.ng-panel { background: #fff; border: 1px solid #e2e8f0; border-radius: 10px; padding: 14px 16px; margin: 12px 0; }
+.ng-panel-title { font-weight: 600; margin-bottom: 10px; color: #0f172a; }
+.ng-cols { display: flex; gap: 12px; flex-wrap: wrap; }
+.ng-cols > .ng-panel { flex: 1; min-width: 280px; }
+.ng-doc-link { display: block; width: 100%; text-align: left; background: #f8fafc; border: 1px solid #e2e8f0; border-radius: 8px; padding: 8px 10px; margin-bottom: 6px; cursor: pointer; }
+.ng-doc-link:hover { background: #f1f5f9; }
+.ng-doc-link-title { font-size: 13px; font-weight: 600; color: #0f172a; }
+.ng-doc-link-meta { font-size: 11px; color: #94a3b8; }
+.ng-event { display: flex; gap: 8px; align-items: center; font-size: 13px; margin-bottom: 6px; }
+.ng-sev { font-size: 11px; border-radius: 6px; padding: 1px 8px; border: 1px solid; }
+.ng-sev-critical { color: #b91c1c; border-color: #fca5a5; background: #fef2f2; }
+.ng-sev-warning { color: #b45309; border-color: #fcd34d; background: #fffbeb; }
+.ng-sev-info { color: #0369a1; border-color: #7dd3fc; background: #f0f9ff; }
+.ng-form { display: flex; gap: 12px; align-items: flex-end; flex-wrap: wrap; }
+.ng-form label { display: flex; flex-direction: column; font-size: 12px; color: #64748b; gap: 4px; }
+.ng-form select, .ng-form input { border: 1px solid #cbd5e1; border-radius: 8px; padding: 7px 10px; font-size: 13px; min-width: 120px; background: #fff; }
+.ng-btn { border: 1px solid #cbd5e1; background: #fff; border-radius: 8px; padding: 8px 14px; font-size: 13px; cursor: pointer; }
+.ng-btn.primary { background: #0f172a; color: #fff; border-color: #0f172a; }
+.ng-btn:disabled { opacity: 0.5; cursor: default; }
+.ng-mape { border-radius: 10px; padding: 12px; margin-bottom: 10px; border: 1px solid; }
+.ng-mape.ok { background: #ecfdf5; border-color: #6ee7b7; }
+.ng-mape.warn { background: #fffbeb; border-color: #fcd34d; }
+.ng-table { width: 100%; border-collapse: collapse; font-size: 13px; }
+.ng-table th, .ng-table td { text-align: left; padding: 5px 8px; border-bottom: 1px solid #f1f5f9; }
+.ng-table th { color: #64748b; font-size: 12px; }
+.ng-table tr.best { background: #ecfdf5; font-weight: 600; }
+.ng-doc-meta { font-size: 12px; color: #94a3b8; margin-bottom: 10px; }
+
+/* 나노그리드 — 메뉴 개편/신규 페이지 */
+.ng-children { margin-left: 10px; border-left: 1px solid rgba(255,255,255,0.1); }
+.ng-doc2 { padding-left: 30px; font-size: 12px; }
+.ng-subtabs { display: flex; gap: 8px; margin: 4px 0 12px; }
+.ng-subtab { border: 1px solid #cbd5e1; background: #fff; border-radius: 999px; padding: 6px 14px; font-size: 13px; cursor: pointer; color: #475569; }
+.ng-subtab.active { background: #0f172a; color: #fff; border-color: #0f172a; }
+.ng-ev-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(230px, 1fr)); gap: 12px; }
+.ng-ev-card { border: 1px solid #e2e8f0; border-radius: 10px; padding: 12px 14px; background: #fff; }
+.ng-ev-card.ng-ev-charging { border-color: #6ee7b7; background: #f0fdf9; }
+.ng-ev-card.ng-ev-fault { border-color: #fca5a5; background: #fef2f2; }
+.ng-ev-card.ng-ev-finishing { border-color: #93c5fd; background: #eff6ff; }
+.ng-ev-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 4px; }
+.ng-ev-name { font-weight: 600; font-size: 14px; color: #0f172a; }
+.ng-ev-status { font-size: 12px; color: #475569; }
+.ng-ev-power { font-size: 20px; font-weight: 700; color: #0f172a; }
+.ng-ev-bar { height: 6px; border-radius: 3px; background: #e2e8f0; margin: 6px 0; overflow: hidden; }
+.ng-ev-bar-fill { height: 100%; background: linear-gradient(90deg, #10b981, #059669); transition: width 0.6s; }
+.ng-textarea { width: 100%; border: 1px solid #cbd5e1; border-radius: 8px; padding: 10px; font-size: 13px; font-family: inherit; margin: 10px 0; resize: vertical; }
+.ng-link { background: none; border: none; color: #4338ca; cursor: pointer; font-size: 13px; padding: 0; text-decoration: underline; }
+.ng-gov-group { margin-bottom: 14px; }
+.ng-gov-cat { font-weight: 700; font-size: 13px; color: #4338ca; margin: 8px 0 6px; }
+.ng-gov-item { display: flex; gap: 10px; align-items: flex-start; border: 1px solid #e2e8f0; border-radius: 8px; padding: 10px; margin-bottom: 6px; background: #fff; }
+.ng-gov-item.done { background: #f0fdf4; border-color: #bbf7d0; }
+.ng-gov-item.na { opacity: 0.55; }
+.ng-gov-item input[type="checkbox"] { margin-top: 3px; }
+.ng-gov-item select { border: 1px solid #cbd5e1; border-radius: 6px; font-size: 12px; padding: 3px 6px; }
+.ng-gov-title { font-size: 13px; font-weight: 600; color: #0f172a; }
+.ng-gov-quote { font-size: 12px; color: #64748b; background: #f8fafc; border-left: 3px solid #c7d2fe; padding: 4px 8px; margin-top: 4px; }
+.banner { background: #eef2ff; border: 1px solid #c7d2fe; color: #3730a3; border-radius: 8px; padding: 8px 12px; margin: 8px 0; font-size: 13px; }
+.banner.error { background: #fef2f2; border-color: #fecaca; color: #b91c1c; }
+
+/* 사이드바 모드 탭 (소스 분석 / 데이터 지식화) */
+.ng-mode-tabs { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; margin: 4px 0 12px; }
+.ng-mode-tab { text-align: left; border: 1px solid rgba(255,255,255,0.12); background: rgba(255,255,255,0.04); border-radius: 10px; padding: 10px 12px; cursor: pointer; color: #cbd5e1; transition: background 0.15s; }
+.ng-mode-tab:hover { background: rgba(255,255,255,0.08); }
+.ng-mode-tab.active { background: #5b5bd6; border-color: #6e6ade; color: #fff; }
+.ng-mode-title { font-weight: 700; font-size: 14px; }
+.ng-mode-sub { font-size: 11px; opacity: 0.75; margin-top: 3px; line-height: 1.4; }
+.ng-project { display: flex; align-items: center; gap: 8px; border: 1px solid rgba(255,255,255,0.12); background: rgba(255,255,255,0.04); border-radius: 10px; padding: 8px 12px; margin-bottom: 10px; }
+.ng-project-label { font-size: 12px; color: #94a3b8; flex-shrink: 0; }
+.ng-project select { flex: 1; min-width: 0; background: transparent; border: none; color: #e2e8f0; font-size: 13px; outline: none; }
+.ng-project select option { color: #0f172a; }
+
+/* GnG 로고 (다크 사이드바 위 흰색 칩) */
+.brand-logo { height: 34px; width: auto; background: #fff; border-radius: 8px; padding: 4px 8px; display: block; }
+
+/* 인포그래픽 요소 */
+.ng-flow { width: 100%; height: auto; display: block; }
+.ng-flow-dash { stroke-dasharray: 6 12; animation: ngflow 1.2s linear infinite; }
+@keyframes ngflow { to { stroke-dashoffset: -18; } }
+.ng-kpi-row { display: flex; align-items: center; justify-content: space-between; gap: 8px; }
+.ng-donut { text-align: center; flex-shrink: 0; }
+.ng-donut-sub { font-size: 11px; color: #64748b; margin-top: 2px; }
+.ng-cat-bar { display: flex; align-items: center; gap: 10px; margin-bottom: 6px; }
+.ng-cat-bar-label { width: 130px; font-size: 12px; color: #475569; flex-shrink: 0; }
+.ng-cat-bar-track { flex: 1; height: 10px; border-radius: 5px; background: #eef2f7; overflow: hidden; }
+.ng-cat-bar-fill { height: 100%; border-radius: 5px; transition: width 0.5s; }
+.ng-cat-bar-value { width: 70px; font-size: 12px; font-weight: 600; color: #0f172a; text-align: right; flex-shrink: 0; }
